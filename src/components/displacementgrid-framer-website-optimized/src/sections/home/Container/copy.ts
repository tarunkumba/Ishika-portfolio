/** The repeated copy in this section: every visible string, and every link.
 *
 *  Framer inlines one subtree per breakpoint — desktop, tablet, phone — so
 *  each of these appears once per breakpoint in the markup. Edit it here and
 *  all of them change. */
export const copy = {
  thisBackgroundEffectWon: "This background effect won't work on mobile since it needs to track the cursor.",
  displacementGrid: "Displacement Grid",
  example1: "Example 1",
  linkStyle2: "./style-2",
  example2: "Example 2",
  linkStyle3: "./style-3",
  example3: "Example 3",
} as const;

